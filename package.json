{
  "name": "make-me-a-content",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "rome check",
    "lint:fix": "rome check --apply",
    "test": "exit 1",
    "preversion": "rm -rf ./dist && npm run build && npm run lint",
    "postversion": "npm publish && git push --follow-tags"
  },
  "author": "antonk52",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.17.37",
    "rome": "^10.0.4-beta",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "escape-string-regexp": "^4.0.0"
  }
}
